node_modules
package-lock.json
# Include `package-lock.json` file for my `thoughts-principles-react` project
!thoughts-principles-react/package-lock.json
.secrets-for-act

# Ruby 
*.gem
# When we run live reload server with `sudo bundle exec jekyll serve --livereload` it outputs to `_site` directory.
_site