#!/usr/bin/env perl
use warnings;
print("hello world\n");

$x = 10;
$y = 20;
$s = "Perl string";

$c = $x + $y;
print($c);

print($s);

# It is called pragma in Perl. This pragma instructs Perl to turn on additional warning reporting.
# Singel quotes work but double quotes are flawless.
# CONTINUE PERL TUTORIAL FROM: https://www.perltutorial.org/perl-variables/
